class User extends Component { 
  constructor(props){
    super(props);
    // convert raw js object this.movie to a real instance of Movie
    
    // a user may not have one booking
    // this.booking = new Booking(this.booking);
  }
}